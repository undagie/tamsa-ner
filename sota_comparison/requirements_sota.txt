# SOTA comparison experiments (IDNer2k, same splits & metrics)
# Install from project root: pip install -r sota_comparison/requirements_sota.txt

seqeval>=1.2.2
numpy>=1.20.0

# GLiNER (zero-shot or fine-tune)
gliner>=0.2.0
gliner[training]>=0.2.0

# NusaBERT / HuggingFace (inference and fine-tuning)
transformers>=4.20.0,<5.0.0
torch>=1.12.0
accelerate>=0.20.0
datasets>=2.0.0
